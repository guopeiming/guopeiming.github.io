source "https://rubygems.org"
# Hello! This is where you manage which Jekyll version is used to run.
# When you want to use a different version, change it below, save the
# file and run `bundle install`. Run Jekyll with `bundle exec`, like so:
#
#     bundle exec jekyll serve
#
# This will help ensure the proper Jekyll version is running.
# Happy Jekylling!
# gem "jekyll", "~> 4.4.1"
# This is the default theme for new Jekyll sites. You may change this to anything you like.

gem 'github-pages', group: :jekyll_plugins

# 编辑以 # gem "github-pages" 开头的行，以添加 github-pages gem。 将此行更改为：
# gem "github-pages", "~> GITHUB-PAGES-VERSION", group: :jekyll_plugins
# 将 GITHUB-PAGES-VERSION 替换为 github-pages gem 的最新支持版本。 可以在 https://pages.github.com/versions 找到这个版本：依赖项版本。
# https://docs.github.com/zh/pages/setting-up-a-github-pages-site-with-jekyll/creating-a-github-pages-site-with-jekyll
